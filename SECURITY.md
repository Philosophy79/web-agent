# 安全策略（SECURITY）

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 1.x（最新） | ✅ 积极维护 |
| < 1.0 | ❌ 不支持 |

## 报告漏洞

如果你发现安全漏洞，请**不要**公开提交 Issue。请通过以下方式私下报告：

1. 在 GitHub 上打开一个 Issue，标题标注 `[SECURITY]`，正文只描述影响范围，
   **不要**包含可直接利用的细节（我们会在 48 小时内回复并私下沟通细节）
2. 我们确认修复后会发布补丁版本，并在 CHANGELOG 中致谢报告者

## 本工具的安全设计

- **本地优先**：网页内容、转录、识图全部在本机处理，无任何数据上传行为（见 docs/PRIVACY.md）
- **密码不落盘**：所有密码由用户本人输入，工具不接触、不保存；操作日志不含凭据
- **危险动作熔断**：密码/支付/转账/删除/注销等操作默认拒绝，需显式 `--approve`
- **临时凭据即用即删**：个别站点下载所需的临时 Cookie 采集自用户本机浏览器，用完立即删除
- **服务仅监听本机**：视觉服务绑定 127.0.0.1，外部网络不可访问

## 使用者的安全建议

- 只对**自己有权访问**的内容使用本工具（见 docs/COMPLIANCE.md）
- 下载的转录稿可能包含他人个人信息，分享前请脱敏
- 在共享电脑上使用时，注意 `downloads/`、`output/` 中的本地产物
